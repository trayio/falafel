
module.exports = function (triggerFn) {
	return triggerFn;
	// return function (req, res, metadata, reqMetadata, triggerWorkflow) {

	// 	parseBody(req.body)

	// 	.then(function () {
	// 		return triggerFn(req, res, metadata, reqMetadata, triggerWorkflow);
	// 	})


	// };
};